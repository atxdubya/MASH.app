<h1>Editing spouse</h1>

<%= form_for :spouse, url: spouse_path(@spouse), method: :patch do |f| %>

  <% if @spouse.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@spouse.errors.count, "error") %> prohibited
        this spouse from being saved:
      </h2>
      <ul>
        <% @spouse.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>

  <p>
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </p>

  <p>
    <%= f.submit %>
  </p>

<% end %>

<%= link_to 'Back', spouses_path %>
